#include <iostream>
#include <map>

using namespace std;

int main()
{
	map<string, string> objects;
	string one = "shoe";
	string two = "legs";
	
	if(objects[one] == two);
	cout << objects[one] << endl;
	
	return 0;
}
